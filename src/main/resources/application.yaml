spring:
  application:
    name: rewards
  datasource:
    url: jdbc:postgresql://localhost:5432/rewards_db
    username: postgres
    password: Dbpas123*

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration